name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
    - run: docker login "cheastuscr.azurecr.io"  -u "cheastuscr" -p "KoUf7A/gvqfLpxlmejEkxnLkGsvEjOrYrB/HWPzipM+ACRBqdLoT"
    - run: docker build . --file Dockerfile --tag chforum:latest
    - run: docker push cheastuscr.azurecr.io/chforum
